import React from "react";
// import { useGetSellerPropertiesQuery } from "../../redux/features/seller/sellerApi";

const SellerProperties = () => {
  //   const { data, isLoading } = useGetSellerPropertiesQuery();

  //   if (isLoading) return <p>Loading properties...</p>;

  return (
    <div>
      <h1 className="text-3xl font-bold mb-4">Your Properties</h1>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        hhhhhhhhhhhhhhhh
      </div>
    </div>
  );
};

export default SellerProperties;
